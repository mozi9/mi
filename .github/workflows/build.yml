name: Build Kernel

on:
  workflow_dispatch:
    inputs:
      device:
        description: '选择设备代号'
        required: true
        default: 'alioth'
        type: choice
        options:
          - psyche
          - umi
          - munch
          - lmi
          - cmi
          - cas
          - apollo
          - alioth
          - elish
          - enuma
          - dagu
          - pipa
      build_type:
        description: '选择构建类型'
        required: true
        default: 'miui'
        type: choice
        options:
          - 'aosp'
          - 'miui'
          - 'all'
      include_ksu:
        description: '是否包含KSU'
        required: true
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 120

    steps:
    - name: Checkout source
      uses: actions/checkout@v4
      with:
        fetch-depth: 1

    - name: Clean previous build artifacts
      run: |
        echo "🧹 清理之前的构建产物..."
        # 只清理构建产物，不删除源代码
        sudo rm -rf out/ anykernel/ *.zip .dts.bak/ 2>/dev/null || true
        # 清理内核编译临时文件
        sudo find . -name "*.o" -delete 2>/dev/null || true
        sudo find . -name "*.cmd" -delete 2>/dev/null || true
        sudo find . -name ".tmp_*" -type d -exec rm -rf {} + 2>/dev/null || true
        sudo find . -name ".*.cmd" -delete 2>/dev/null || true
        sudo find . -name "*.mod" -delete 2>/dev/null || true
        sudo find . -name "*.mod.c" -delete 2>/dev/null || true
        sudo find . -name "*.order" -delete 2>/dev/null || true
        sudo find . -name "*.ko" -delete 2>/dev/null || true
        sudo find . -name "modules.builtin" -delete 2>/dev/null || true
        sudo find . -name "Module.symvers" -delete 2>/dev/null || true
        
        echo "📁 清理后目录状态:"
        ls -la
        echo ""

    - name: Install dependencies
      run: |
        sudo apt update
        sudo apt install -y build-essential git curl wget bison flex zip bc cpio libssl-dev ccache python-is-python3 tar file

    # 生成短哈希
    - name: Set short commit SHA
      run: echo "SHORT_SHA=$(echo ${GITHUB_SHA} | cut -c1-8)" >> $GITHUB_ENV

    - name: Cache ccache
      uses: actions/cache@v4
      with:
        path: ~/.cache/ccache_mikernel
        key: ${{ runner.os }}-ccache-${{ github.event.inputs.device }}-${{ github.event.inputs.build_type }}-${{ github.event.inputs.include_ksu }}-${{ env.SHORT_SHA }}
        restore-keys: |
          ${{ runner.os }}-ccache-${{ github.event.inputs.device }}-${{ github.event.inputs.build_type }}-${{ github.event.inputs.include_ksu }}-
          ${{ runner.os }}-ccache-${{ github.event.inputs.device }}-${{ github.event.inputs.build_type }}-
          ${{ runner.os }}-ccache-${{ github.event.inputs.device }}-
          ${{ runner.os }}-ccache-

    - name: Setup zyc-clang toolchain
      run: |
        echo "🔧 设置工具链..."
        mkdir -p $HOME/zyc-clang
        cd $HOME/zyc-clang
        wget -q https://github.com/ZyCromerZ/Clang/releases/download/15.0.7-20251111-release/Clang-15.0.7-20251111.tar.gz
        tar -zxf Clang-15.0.7-20251111.tar.gz
        cd $GITHUB_WORKSPACE
        echo "✅ 工具链设置完成"

    - name: Verify environment
      run: |
        echo "🔍 验证构建环境..."
        
        # 检查必要文件
        echo "📁 检查必要文件..."
        if [ ! -f "./build.sh" ]; then
          echo "❌ 错误: build.sh 文件不存在"
          echo "💡 请确保仓库包含构建脚本"
          echo "📋 当前目录内容:"
          ls -la
          exit 1
        fi
        
        if [ ! -d "arch/arm64/configs" ]; then
          echo "❌ 错误: 内核配置目录不存在"
          echo "💡 请检查仓库结构是否正确"
          echo "📋 当前目录结构:"
          find . -maxdepth 2 -type d | sort
          exit 1
        fi
        
        # 检查设备配置
        CONFIG_FILE="arch/arm64/configs/${{ github.event.inputs.device }}_defconfig"
        if [ ! -f "$CONFIG_FILE" ]; then
          echo "❌ 错误: 设备 [${{ github.event.inputs.device }}] 的配置文件不存在: $CONFIG_FILE"
          echo "📋 可用设备配置:"
          ls -la arch/arm64/configs/*_defconfig 2>/dev/null | sed 's|.*/||; s|_defconfig||' || echo "   无可用配置"
          exit 1
        fi
        
        echo "✅ 环境验证通过"

    - name: Build kernel
      run: |
        set -e  # 遇到错误立即退出
        
        echo "🚀 开始构建内核..."
        echo "📋 构建配置:"
        echo "   设备: ${{ github.event.inputs.device }}"
        echo "   类型: ${{ github.event.inputs.build_type }}"
        echo "   KSU: ${{ github.event.inputs.include_ksu }}"
        
        # 设置环境变量
        export TOOLCHAIN_PATH="$HOME/zyc-clang"
        export PATH="$HOME/zyc-clang/bin:$PATH"
        
        # 验证工具链
        echo "🔧 验证工具链..."
        if ! command -v clang >/dev/null 2>&1; then
          echo "❌ 错误: clang 工具链未正确设置"
          echo "💡 请检查 TOOLCHAIN_PATH: $TOOLCHAIN_PATH"
          echo "📋 工具链目录内容:"
          ls -la $TOOLCHAIN_PATH/bin/ 2>/dev/null || echo "工具链目录为空"
          exit 1
        fi
        
        # 显示工具链版本
        echo "📋 工具链信息:"
        clang --version | head -1 || echo "无法获取clang版本"
        
        # 构建命令参数
        BUILD_ARGS="${{ github.event.inputs.device }}"
        
        # 添加KSU参数
        if [ "${{ github.event.inputs.include_ksu }}" = "true" ]; then
          BUILD_ARGS="$BUILD_ARGS ksu"
        fi
        
        # 添加构建类型参数
        if [ "${{ github.event.inputs.build_type }}" = "aosp" ]; then
          BUILD_ARGS="$BUILD_ARGS --aosp"
        elif [ "${{ github.event.inputs.build_type }}" = "miui" ]; then
          BUILD_ARGS="$BUILD_ARGS --miui"
        elif [ "${{ github.event.inputs.build_type }}" = "all" ]; then
          BUILD_ARGS="$BUILD_ARGS --aosp --miui"
        fi
        
        chmod +x ./build.sh
        
        # 执行构建
        echo "🔨 执行构建命令: ./build.sh $BUILD_ARGS"
        if ./build.sh $BUILD_ARGS; then
          echo "✅ 构建成功完成"
        else
          BUILD_EXIT_CODE=$?
          echo "❌ 构建失败，退出代码: $BUILD_EXIT_CODE"
          echo "🔍 详细错误分析:"
          
          # 根据退出代码提供具体错误信息
          case $BUILD_EXIT_CODE in
            1)
              echo "💡 错误类型: 参数或配置错误"
              echo "   可能原因:"
              echo "   - 设备 [${{ github.event.inputs.device }}] 配置不存在"
              echo "   - 构建参数不匹配"
              echo "   - 环境变量设置错误"
              ;;
            2)
              echo "💡 错误类型: 编译错误"
              echo "   可能原因:"
              echo "   - 代码冲突或语法错误"
              echo "   - 头文件缺失"
              echo "   - 工具链不兼容"
              ;;
            127)
              echo "💡 错误类型: 命令未找到"
              echo "   可能原因: build.sh 脚本不存在或没有执行权限"
              ;;
            *)
              echo "💡 错误类型: 未知错误 (退出代码: $BUILD_EXIT_CODE)"
              echo "   可能原因: 内存不足、磁盘空间不足或其他系统错误"
              ;;
          esac
          
          # 检查构建状态
          echo "📁 检查构建状态:"
          if [ -d "out" ]; then
            echo "   ✅ out/ 目录存在"
            if [ -f "out/arch/arm64/boot/Image" ]; then
              echo "   ✅ 内核镜像已生成"
              echo "   📏 镜像大小: $(ls -lh out/arch/arm64/boot/Image | awk '{print $5}')"
            else
              echo "   ❌ 内核镜像未生成"
              echo "   📋 out/arch/arm64/boot/ 目录内容:"
              ls -la out/arch/arm64/boot/ 2>/dev/null || echo "   目录不存在"
            fi
          else
            echo "   ❌ out/ 目录不存在，编译可能未开始"
          fi
          
          # 检查配置状态
          echo "🔧 检查配置状态:"
          if [ -f "out/.config" ]; then
            echo "   ✅ 配置已生成"
            # 检查关键配置
            if ${{ github.event.inputs.include_ksu == 'true' }} && grep -q "CONFIG_KSU=y" out/.config 2>/dev/null; then
              echo "   ✅ KSU配置已启用"
            elif ${{ github.event.inputs.include_ksu == 'true' }}; then
              echo "   ❌ KSU配置未启用"
            fi
          else
            echo "   ❌ 配置未生成，defconfig可能有问题"
          fi
          
          exit $BUILD_EXIT_CODE
        fi

    - name: Clean build artifacts after success
      if: success()
      run: |
        echo "🧹 构建成功，清理中间文件..."
        # 保留刷机包，清理其他中间文件
        sudo rm -rf out/ anykernel/ .dts.bak/ 2>/dev/null || true
        sudo find . -name "*.o" -delete 2>/dev/null || true
        sudo find . -name "*.cmd" -delete 2>/dev/null || true
        sudo find . -name ".tmp_*" -type d -exec rm -rf {} + 2>/dev/null || true
        
        echo "📁 最终产物:"
        ls -la *.zip 2>/dev/null && echo "✅ 刷机包已生成" || echo "❌ 未找到刷机包"

    - name: Clean build artifacts on failure
      if: failure()
      run: |
        echo "🧹 构建失败，保留调试文件..."
        # 失败时保留out目录用于调试
        echo "📁 保留调试文件用于分析"
        if [ -d "out" ]; then
          echo "📏 out目录大小: $(du -sh out | cut -f1)"
        fi

    - name: Show ccache stats
      run: ccache -s

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ${{ github.event.inputs.device }}-${{ github.event.inputs.build_type }}${{ github.event.inputs.include_ksu == 'true' && '-ksu' || '' }}-kernel
        path: |
          *.zip
        retention-days: 7

    - name: Upload error logs on failure
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: ${{ github.event.inputs.device }}-error-debug
        path: |
          out/
          arch/arm64/configs/${{ github.event.inputs.device }}_defconfig
        retention-days: 3
      continue-on-error: true
